# ARANDU ENCODER — Dependencias
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scikit-learn>=1.2.0
tqdm>=4.65.0
pyyaml>=6.0
Pillow>=9.5.0
opencv-python>=4.7.0          # corrupt_dataset.py, visualize_alpha.py
matplotlib>=3.7.0             # visualize_alpha.py, evaluate_downstream.py
seaborn>=0.12.0               # evaluate_downstream.py (confusion matrix)
ultralytics>=8.0.0            # ablation_runner.py, visualize_alpha.py, evaluate_alpha_dataset.py

# Opcionales (mejoran rendimiento pero no son requeridas)
# wandb              # Logging con Weights & Biases
# faiss-gpu          # KNN acelerado por GPU (fallback: sklearn)
